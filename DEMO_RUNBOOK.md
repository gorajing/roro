# Roro Demo Runbook

The current Roro setup and live-run path are documented in [`README.md`](README.md)
and [`RUN.md`](RUN.md). Public-readiness gates live in [`PUBLIC.md`](PUBLIC.md).

The old 2026 hackathon cloud-demo runbook (a predecessor hosted demo stack, not the
current local-first release path) lives in git history: `git show
feaad68:docs/archive/DEMO_RUNBOOK.md`. Its one durable fact: the original wow moment
was proven end-to-end on stage hardware — decide → real Codex → `calc.py` fixed →
tests pass, measured at **78.9s red→green** — before the local-first rebuild.
